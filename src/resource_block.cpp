#include "resource_block.h"

namespace telecom {

// ResourceBlock and ResourceBlockHandle are header-only data types.
// This translation unit exists to satisfy the CMake build target.

} // namespace telecom
